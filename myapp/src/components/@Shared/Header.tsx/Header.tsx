import { HeaderContainer, HeaderList } from './HeaderStyle';

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderList>
        <div>로고</div>
        <div>로그인</div>
      </HeaderList>
    </HeaderContainer>
  );
};

export default Header;
